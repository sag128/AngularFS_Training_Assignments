import { AdminPanelDirective } from './admin-panel.directive';

describe('AdminPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminPanelDirective();
    expect(directive).toBeTruthy();
  });
});
